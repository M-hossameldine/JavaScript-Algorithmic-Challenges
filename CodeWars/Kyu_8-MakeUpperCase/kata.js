/*
Write a function which converts the input string to uppercase.
*/

function makeUpperCase(string) {
  return string.toUpperCase();
}

console.log(makeUpperCase("alaa"));    // -> ALAAN
console.log(makeUpperCase("teSt"));    // -> TEST